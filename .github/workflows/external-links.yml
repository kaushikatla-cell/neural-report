name: External links (lychee)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 15 * * 1"
  push:
    paths:
      - "docs/**"
      - "lychee.toml"

jobs:
  lychee:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check external URLs in HTML
        uses: lycheeverse/lychee-action@v1
        with:
          args: --config lychee.toml --no-progress './docs/**/*.html'
        continue-on-error: true
        # Advisory schedule: failures do not block. For a failing (strict) run, see citation-freshness.yml (Wednesdays).
        # Re-run locally: lychee --config lychee.toml './docs/**/*.html'
